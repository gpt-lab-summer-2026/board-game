"""The speaking half of the ghost, kept out of PlanarAlly's tree.

`ghost/narrate.py` looks for `voice.tts.KokoroSpeaker` here and shrugs if it is
absent, so the board can be driven from a terminal with no audio stack at all.
"""
